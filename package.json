{
  "name": "maawen",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "المعاون — assistant IA personnel (Supabase + Android). Outils de test uniquement: le code de production tourne sur Deno (Edge Functions) et sur Android.",
  "scripts": {
    "test": "node --test --test-concurrency=1 supabase/tests/*.test.ts",
    "test:db": "supabase/tests/run.sh"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.124.0",
    "@supabase/supabase-js": "^2.58.0",
    "pg": "^8.13.0"
  }
}
